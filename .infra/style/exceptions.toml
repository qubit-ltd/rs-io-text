format = 1

[[exceptions]]
rule = "coverage-cfg"
path = "src/adapters/async_charset_text_reader.rs"
reason = "Inject EOF status branches so asynchronous decoder recovery paths can be exercised under coverage builds."

[[exceptions]]
rule = "coverage-cfg"
path = "src/adapters/charset_string_decoder.rs"
reason = "Inject reserve failure hooks so allocation error mapping can be exercised under coverage builds."

[[exceptions]]
rule = "coverage-cfg"
path = "src/adapters/charset_string_encoder.rs"
reason = "Inject reserve failure hooks so allocation error mapping can be exercised under coverage builds."

[[exceptions]]
rule = "coverage-cfg"
path = "src/stream/buffered_reader.rs"
reason = "Inject decoder status and refill branches so buffered reader coverage thresholds include recovery paths."

[[exceptions]]
rule = "coverage-cfg"
path = "src/stream/buffered_writer.rs"
reason = "Inject a reset reserve failure so the recoverable allocation error path can be exercised under coverage builds."
